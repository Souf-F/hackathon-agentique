# Corpus de démo

Un fichier `.txt` par CV, paragraphes séparés par une ligne vide (c'est l'unité de chunk utilisée par `backend/pipeline.py`).

Prévu : 4 CV légitimes + 1 CV contenant une injection cachée (texte adressé à l'IA de tri plutôt qu'au lecteur humain — cf. SPEC.md section 9). Contenu à ajouter séparément, généré par un autre outil IA que celui qui construit la détection.
